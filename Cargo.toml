[package]
name = "hulk-compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
chumsky = "0.13.0"
logos = "0.15"
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
insta = { version = "1.47.2", features = ["json", "yaml"] }
